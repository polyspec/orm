{
    "name": "orm/php-client",
    "description": "PHP executor of the orm compiler: generated typed builders over PDO, plans compiled by ormd",
    "type": "library",
    "version": "0.0.1",
    "license": "proprietary",
    "require": {
        "php": ">=8.2",
        "ext-pdo": "*",
        "ext-pdo_mysql": "*",
        "ext-json": "*",
        "ext-zlib": "*"
    },
    "suggest": {
        "ext-apcu": "shares compiled plans across PHP-FPM workers (without it every process asks ormd once per statement shape)",
        "ext-pdo_pgsql": "Db::postgres (ormd -dialect postgres)",
        "ext-pdo_sqlite": "Db::sqlite (ormd -dialect sqlite)",
        "ext-openssl": "host-side AES on PostgreSQL and SQLite (aes columns)"
    },
    "autoload": {
        "psr-4": {
            "Orm\\": "src/"
        },
        "classmap": [
            "src/"
        ]
    },
    "config": {
        "optimize-autoloader": true
    }
}
