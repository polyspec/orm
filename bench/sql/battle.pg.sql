-- generated by ormgen ddl (postgres) from schema_hash 2bdd94841cfad48c

DROP TABLE IF EXISTS "battle";
CREATE TABLE "battle" (
  "seq" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  "name" varchar(191) NOT NULL,
  "description" text,
  "created_ts" timestamp(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_ts" timestamp(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "is_close" boolean NOT NULL DEFAULT false,
  "is_display" boolean NOT NULL DEFAULT false,
  "display_start_dt" timestamp(6),
  "display_end_dt" timestamp(6),
  "is_allday" boolean NOT NULL DEFAULT false,
  "target_team_player_count" bigint NOT NULL DEFAULT 0,
  "success_count" bigint NOT NULL DEFAULT 0,
  "player_count" bigint NOT NULL DEFAULT 0,
  "read_count" bigint NOT NULL DEFAULT 0,
  "cover_url" varchar(191),
  "user_seq" bigint NOT NULL,
  "service_seq" bigint NOT NULL,
  "service_module_seq" bigint NOT NULL,
  "service_member_seq" bigint NOT NULL,
  "start_dt" timestamp(6) NOT NULL,
  "end_dt" timestamp(6) NOT NULL,
  "uuid" varchar(36),
  "is_single_play" boolean NOT NULL DEFAULT false,
  "like_count" bigint NOT NULL DEFAULT 0,
  "aes_hex_email" varchar(255),
  "aes_hex_phone" varchar(255),
  "price" numeric(13,3),
  "ip" inet,
  "gz_extend" bytea,
  "json_setting" jsonb,
  "jsons_tags" text,
  "base64_extra" text,
  "serialize_data" text,
  PRIMARY KEY ("seq"),
  UNIQUE ("uuid")
);
CREATE INDEX "battle_ix_service" ON "battle" ("service_seq", "is_close");
CREATE INDEX "battle_ix_user" ON "battle" ("user_seq", "is_close");
CREATE INDEX "battle_ik" ON "battle" ("service_module_seq", "is_close", "is_display", "is_allday");
CREATE INDEX "battle_ft_name_description" ON "battle" USING GIN (to_tsvector('simple', coalesce("name", '') || ' ' || coalesce("description", '')));

DROP TABLE IF EXISTS "user";
CREATE TABLE "user" (
  "seq" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  "name" varchar(191) NOT NULL,
  PRIMARY KEY ("seq")
);

DROP TABLE IF EXISTS "service";
CREATE TABLE "service" (
  "seq" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  "name" varchar(191) NOT NULL,
  PRIMARY KEY ("seq")
);

DROP TABLE IF EXISTS "service_module";
CREATE TABLE "service_module" (
  "seq" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  "service_seq" bigint NOT NULL,
  "name" varchar(191) NOT NULL,
  PRIMARY KEY ("seq")
);

DROP TABLE IF EXISTS "service_member";
CREATE TABLE "service_member" (
  "seq" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  "service_seq" bigint NOT NULL,
  "user_seq" bigint NOT NULL,
  PRIMARY KEY ("seq")
);
